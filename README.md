# Harbor Runtime E2E

Temporary, checksum-pinned Harbor 0.6.6 runtime verification. The workflow runs an Oracle trial and a credential-free custom Agent trial against the same Docker task and verifier.
